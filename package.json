{
  "name": "taskrewards",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "start": "lerna run start",
    "lint": "lerna run lint",
    "gen": "lerna run gen",
    "deploy:localhost": "lerna run deploy:localhost",
    "deploy:goerli": "deploy:goerli"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
